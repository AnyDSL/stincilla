extern "C" {
    fn drand48() -> f64;
    fn srand48(i64) -> ();
}

fn iteration(out: Buffer, width: i32, height: i32,
             body: fn(i32, i32, Buffer, fn() -> f64) -> ()
            ) -> () {
    srand48(thorin_get_micro_time());

    for benchmark_cpu() {
        for y in $outer_loop(0, height) {
            for x in $inner_loop(0, width) @{
                fn rand() -> f64 { drand48() }
                body(x, y, out, rand);
            }
        }
    }
}
